import styles from "./footer.module.css";
import React, { useRef, useEffect } from "react";

function Footer() {
  return (
    <section className={styles.footer} href="#">
      <h1>Footer</h1>
    </section>
  );
}

export default Footer;
