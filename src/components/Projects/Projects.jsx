import styles from "./projects.module.css";
import React, { useRef, useEffect } from "react";

function Projects() {
  return (
    <section className={styles.projects} href="#">
      <h1>Projects</h1>
    </section>
  );
}

export default Projects;
